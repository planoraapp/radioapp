{
  "buildCommand": "cd web && CI=false GENERATE_SOURCEMAP=false npm run build",
  "outputDirectory": "web/build",
  "installCommand": "cd web && npm install",
  "framework": null,
  "env": {
    "CI": "false",
    "GENERATE_SOURCEMAP": "false"
  }
}
